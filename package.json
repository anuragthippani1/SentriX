{
  "name": "sentrix",
  "version": "1.0.0",
  "description": "SentriX - Multi-Agent Supply Chain Risk Intelligence Platform",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && python -m uvicorn main:app --reload --port 8000",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "keywords": ["supply-chain", "risk-intelligence", "ai-agents", "react", "fastapi"],
  "author": "SentriX Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
